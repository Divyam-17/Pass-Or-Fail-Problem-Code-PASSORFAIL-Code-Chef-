#include <iostream>
using namespace std;

int main() {
    int t;
    cin>>t;
    while(t--){
        int n,x,p;
        cin>>n>>x>>p;
        int marks=(3*x)-(n-x);
        if (marks>=p)
        cout<<"PASS\n";
        else
        cout<<"FAIL\n";
    }
    return 0;
}
